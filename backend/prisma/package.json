{
  "name": "@gifting/prisma",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "generate": "dotenv -e ../.env -- prisma generate",
    "migrate": "dotenv -e ../.env -- prisma migrate dev",
    "seed": "dotenv -e ../.env -- ts-node seed.ts"
  },
  "dependencies": {
    "@prisma/client": "^5.15.0",
    "bcryptjs": "^2.4.3"
  },
  "devDependencies": {
    "@types/bcryptjs": "^3.0.0",
    "dotenv-cli": "^11.0.0",
    "prisma": "^5.15.0",
    "ts-node": "^10.9.2",
    "typescript": "^5.5.0"
  }
}
